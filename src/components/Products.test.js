import { render } from "@testing-library/react";
import React from "react";
import Products from "./products";
describe ("Products",() => {
    it("when handleSearch is true then text should be displayed"()=>{
        render(<Products isDrawerOpen={false} closeDrawer={jest.fn}
    });
    it("when handleSearch is not calling just loading")
    )


;

